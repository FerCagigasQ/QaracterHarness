{
  "name": "apolo-cli",
  "version": "0.1.0-alpha.0",
  "description": "Local terminal-only CLI core for coordinating repository tasks.",
  "type": "module",
  "bin": {
    "apolo": "./dist/index.js"
  },
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "ci": "npm run lint && npm run typecheck && npm test && npm run build && npm run package:check && npm run test:integration && npm run smoke:bin",
    "lint": "tsc -p tsconfig.json --noEmit",
    "package:check": "npm run build && node packaging/validate-npm-package.mjs",
    "smoke:bin": "npm run build && node packaging/smoke-bin.mjs",
    "test": "vitest run tests/cli.test.ts tests/memory-cli.test.ts tests/security-gates.test.ts tests/verification-commands.test.ts",
    "test:integration": "node --test test/integration/*.test.mjs",
    "test:python": "node packaging/test-python-compat.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
